aiortc>=1.14.0
aiohttp>=3.9.0
mss>=10.0.0
numpy>=1.24.0
av>=16.0.0
pyaudiowpatch>=0.2.12.5  # WASAPI loopback — listagem de dispositivos
imageio-ffmpeg>=4.1.0    # FFmpeg binário via pip — captura de áudio
pywin32>=306             # Windows only — cursor capture
PySide6>=6.6.0
